using System;
using System.Collections.Generic;
using System.Text;

namespace JA.Tests
{
    public class IntegrationsTests
    {
        //public int[] RemoveDuplicates()
        //{
        //    int[] inpArr = { 1, 2, 2, 3, 4, 5, 5, 5, 8, 8, 8, 9, 13, 14, 15, 18, 20, 20 };
        //    int[] opArr = new int[inpArr.Length];
        //    int opPos = 0;
        //    for (int i = 0; i = inpArr.Length - 1; i++) { if (inpArr[i] == inpArr[i + 1]) { int repeats = 1; opArr[opPos] = inpArr[i]; int j = i + 1; while (j + 1 & lt);= inpArr.length - 1 & amp; &amp; inpArr[i] == inpArr[j + 1]) { j++; repeats++; } opArr = Arrays.copyOf(opArr, opArr.length - repeats); i = i + repeats; } else { opArr[opPos] = inpArr[i]; } opPos++; }

        //    for (int i = 0; i = opArr.Length - 1; i++)
        //    {
        //        Console.Write(opArr[i] + ","); 

        //    }

        //}
    }
}
